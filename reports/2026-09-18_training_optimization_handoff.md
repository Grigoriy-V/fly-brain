# Передача контекста: ускорение обучения Fly_Brain

Дата: 2026-09-18. Для другого агента, работающего с этим репозиторием.
Это отчёт о разрешениях, действиях и свидетельствах, **не второй roadmap**.
Текущий порядок научных шагов находится только в `ROADMAP.md`.

## Состояние передачи

**GPU benchmark выполняется. Не запускать второй экземпляр.**

- Приложение: https://modal.com/apps/grigoriy98smile/main/ap-Fr0liJXeN4XNScXl9pn1zx
- Профиль: `grigoriy98smile`, GPU: T4.
- Run: `2026-09-18_bench_150043_689575_229624b2a4`.
- Volume: `flydream-runs`, каталог
  `/benchmarks/2026-09-18_bench_150043_689575_229624b2a4/`.
- Один worker, timeout 30 минут. Внутри: warm-up, четыре варианта × два
  повтора, отдельный baseline с профилированием. Варианты последовательны.
- Содержимое каталога подтверждено read-only `modal volume ls`.
- Финальное ускорение, CUDA-эквивалентность и итоговая стоимость пока неизвестны.

Этот раздел будет дополнен исходом того же запуска. Прежде чем продолжать
работу, читать нижний раздел «Итог запуска», если он уже появился, и фактические
артефакты. Отсутствие локального результата не означает, что worker не запущен.

## Что человек разрешил в этом чате

1. «Изучи проект» — анализ без изменений; исследованы документы, код и
   сохранённые измерения, выполнены офлайн-тесты.
2. Вопрос о времени, оптимизации и цене обучения — анализ вариантов.
3. «делай» после предложения подготовить стенд — реализация двух конкретных
   оптимизаций, точного счётчика итераций, профилирования и офлайн-проверок.
4. «давай» после конкретного запроса на T4 benchmark — **один** платный worker
   указанного объёма, оценка 10–20 минут и $0.15–0.35 плюс возможный холодный build.
5. Отдельно попросил этот отчёт, чтобы агенты не расходились в работе.

Полное обучение, новый ансамбль, дополнительные платные retries, L4,
AMP, sparse kernels и смена научного протокола **не разрешены**. Следующий
платный worker требует нового разрешения с командой, временем и ценой
(`AGENTS.md`, Human gates). Не считать данное разрешение бюджетом на серию
новых Modal-вызовов. Read-only получение логов/сохранённых файлов не запускает
обучение и используется для завершения анализа.

## Карта документов и приоритет

- `AGENTS.md`: рабочие правила и human gates.
- `ROADMAP.md`: научный порядок и отдельно отмеченная ограниченная
  инженерная работа. Основной научный шаг 4 не закрыт.
- `docs/PROJECT_MAP.md`, `docs/OPERATIONS_MAP.md`: устройство и операции.
- `reports/2026-09-18_training_optimization_bench.md`: подробный протокол,
  обоснование, офлайн-числа, команда и оценка стоимости текущего эксперимента.
- `reports/runs.jsonl`: измеренные исходы, запись только через `tools/run_log.py`.
- `reports/Обучение коннектомных сетей и ускорение.md`: прежнее исследование;
  его выводы ниже явно отделены от проверенных фактов. Не переписано.

## Что выяснено и какие идеи остаются гипотезами

| Идея / утверждение | Свидетельство и статус |
|---|---|
| Батч 16 выгоднее 4 по throughput | Старый smoke: 1.140 с/итер против 0.434 при четырёхкратном батче. Это не доказательство равного качества при меньшем числе обновлений. Старый счётчик делил на запрошенные, а не реальные итерации. |
| «Запас инженерного ускорения исчерпан, остаётся максимум 10%» | Не принимается как доказанный вывод: три точки времени не являются GPU-профилем. Слияние операций, иной layout и другая карта не ограничены свободным членом линейной подгонки. |
| Статистики активности можно считать на GPU | Подтверждено кодом: штатный solver копирует весь тензор на CPU ради mean/min/max. Реализовано; скорость проверяется текущим worker. |
| ReLU можно перенести перед gather | Подтверждено формулой штатного PPNeuronIGRSynapses и офлайн-сравнением forward/backward. Реализовано; CUDA и скорость проверяются текущим worker. |
| Sparse matrix multiplication / fused gather-reduce | Кандидат после профиля; не реализован, выигрыша не измеряли. Обязательны градиенты по общим параметрам типов, не только forward. |
| torch.compile / CUDA graphs | Кандидат после профиля; не реализован. Ограничение «≤10%» не доказано; источником graph breaks могут быть AutoDeref/RefTensor. |
| AMP / fp16 | Не реализовано. Одного autocast недостаточно для обещания ускорения gather/scatter. Нужны реальные dtype и численная проверка рекуррентной динамики. |
| Ранняя остановка / меньше дообучения из переноса | Возможная экономия по числу обновлений, но данных о сходимости нет. «Нулевая научная цена» неверно: проверить task-loss и биологическую валидацию, с контрольным запуском. |
| L4 | Пока не запускалась здесь. При цене только GPU она должна обогнать T4 примерно в 1.35 раза для безубыточности; CPU/RAM меняют общий порог. Запрет выше L4 сохраняется. |
| Несколько процессов на T4 | Прежние измерения давали небольшой выигрыш; текущий стенд последовательный. Ни обещаний ансамбля, ни новых packed-запусков нет. |

## Реализованные файлы

- `flydream/train/optimizations.py`: обратимые адаптеры одного экземпляра,
  режимы `baseline`, `stats`, `relu`, `stats_relu`; проверка версии FlyVis
  1.2.0 и ожидаемого блока исходного solver. Пакет в `.venv` не редактировался.
- `flydream/train/member.py`: фактическая разность счётчиков iteration,
  CUDA synchronization, отдельные evidence/profiler, seed и выбор варианта.
  По умолчанию остаётся `baseline`; loss, penalties и scheduler сохранены.
- `flydream/train/benchmark.py`: изолированные процессы и новый каталог
  результатов; общий seed; два порядка запуска; отдельный профиль; сравнение
  начальных/конечных состояний и loss-кривых с парным baseline.
- `deploy/modal/train_app.py`: добавлена функция `benchmark`; прежние
  тренировочные функции не запускались в этом чате.
- `config.toml [benchmark]`: batch 16, dt .02, 30 запрошенных итераций,
  seed 0, два повтора, четыре варианта и пределы численной проверки.
- `tests/test_train_optimizations.py`: эквивалентность, восстановление
  адаптеров, реальный цикл solver на синтетическом stub, счётчик resume.
- `tools/verify_train_optimizations.py`: воспроизводимая проверка без
  обновлений optimizer, JSON и график. Повтор с тем же hash не перезаписывает
  результат.
- Обновлены roadmap, карты системы/операций, отчёт и журнал измерений.

## Что уже проверено

- 62 офлайн-теста, 21.93 с; одно прежнее предупреждение в hexconv-тесте.
- `compileall` и `git diff --check` пройдены.
- Run `2026-09-18_optimization_equivalence_b5023898e9`: synthetic mini
  connectome, CPU FP32, batch 2, 40 шагов по .02 с, seed 7.
  Контроль — штатный FlyVis на одинаковых весах и входах.
- Forward: max abs difference **0**. Градиенты свободных параметров:
  max abs difference **5.96e-8**, против нулевой разности эталона с собой.
  Все элементы в пределах `atol=1e-7, rtol=1e-5`; optimizer updates **0**.
- Подробные значения, config и hashes —
  `data/benchmarks/2026-09-18_optimization_equivalence_b5023898e9/result.json`.
- График — `reports/figures/2026-09-18_optimization_equivalence_b5023898e9.png`.
- Числа внесены в `reports/runs.jsonl` через `tools/run_log.py`.

Это эквивалентность синтетического расчёта, **не ускорение GPU, не валидация
MaleCNS и не доказательство равной сходимости обучения**.

## Как прочитать завершённый benchmark

В каталоге Volume искать `manifest.json`, `records.json`, `comparisons.json`,
каталоги вариантов с `result.json`, `stdout.log`, `evidence/{initial,final}.pt`,
`evidence/losses.json`; у отдельного профиля `trace.json` и `operators.txt`.

Время `train_s` включает steady state, logging и checkpoints, исключает
создание solver и дополнительную запись evidence. `s_per_iter` делится на
`completed_iters`, не на запрошенные 30. Скорость профилированного запуска
в сравнение не включать. Два повтора дают разброс, не точный доверительный
интервал. При расхождении чисел сначала сравнить baseline-повторы — CUDA
scatter/reductions могут быть недетерминированными.

В любом исходе сохранить ошибку/числа и контроль в отчёте. Не объявлять
оптимизацию принятой, если численные проверки не прошли. Ошибка worker
не даёт разрешения на ещё один платный запуск.

## Состояние рабочего дерева

Изменения этой работы пока не закоммичены. До начала работ уже был изменён
`reports/figures/2026-09-18_sintel_lag_sweep.png`; его не редактировали и не
включать в собственный commit без выяснения владельца. В репозитории могут
работать другие приложения: текущий агент пишет канонические записи этого
эксперимента; параллельный агент должен использовать отдельную ветку/worktree
и не обновлять эти же записи одновременно.

"""The generator: from an internal state of the model back to the stimulus most
compatible with it (ROADMAP item 8). `invert` is encoder inversion by gradient
descent on the input through the differentiable network."""

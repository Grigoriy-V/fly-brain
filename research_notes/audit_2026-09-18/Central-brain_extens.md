{
 "leg": "Central-brain extension (ROADMAP.md:135-141, roadmap item 6): \"Visual projection neurons and their targets from MaleCNS (LC types, the optic glomeruli, then the central complex) in one differentiable model, no graded-to-spiking seam\" — claim under attack: tractable in the same differentiable model class.",
 "verdict": "COSTLY",
 "headline": "The compute attack fails outright — the whole male brain fits in 17 GB of autograd graph — but the model class survives the move only by inflating from 734 free parameters to 95,265 (the LC-plus-glomeruli scope) or 311,134 (plus the central complex), of which >99.3% have no FlyVis source and 38.9% of central-brain type pairs pool exactly one synaptic edge, against a validation target that does not exist at the model's 50 Hz per-cell-type resolution.",
 "strongest_attack": "Measured on the project's own weights table (data/malecns/connectome-weights-male-cns-v1.0-minconf-0.5.feather, 151,856,684 rows) at the project's own threshold (config.toml:7, min_weight = 5): the MaleCNS central brain has 8,351 cell types across 49,667 cells, median 2 cells per type, 53.2% of types with <=2 cells — against 282 types / 95,455 cells / median 69 in the optic lobe. Per-cell-type parameter sharing, the single thing that makes FlyVis's 734 parameters a constraint rather than a fit, therefore collapses: a central-brain (source type, target type) pair pools a mean of 4.67 body-to-body edges and 38.9% of them pool exactly one, versus a mean of 161.15 in the optic lobe. Roadmap 6's own stated scope is not small: the 346 visual_projection types' direct postsynaptic partners at w>=5 span 3,608 of the 6,313 non-CX cb_intrinsic types (57.2%) — independently corroborated by bioRxiv 2026.02.02.700492 (\"visual inputs to more than half of central-brain cell types\"). \"VPNs and their targets\" is most of the central brain, and the free-parameter count at that scope is 2*1,859 + 91,547 = 95,265 (129.8x FlyVis's 734); with the central complex, 311,134 (423x). At most 604 of those parameters (FlyVis's type pairs, all optic-lobe intrinsic, reports/Коннектом мухи и план проекта.md:38-40) have a source to transplant, so >=99.3% would be initialised at a FlyVis median — and reports/runs.jsonl step2.control records that merely *permuting* the already-transplanted strengths on the existing optic-lobe wiring diverged to inf in 3 of 3 members under both protocols (\"shuffled_strengths_diverged\", value 1.0, n 3), while central-brain cells receive 812 input synapses each on average against the optic lobe's 269 (per-type medians 704 vs 328), so median-filled coefficients multiply into ~2-3x the drive the transplant was tuned for. Beiran & Litwin-Kumar, Nature Neuroscience 28:2561-2574 (2025) then closes the escape: \"Knowledge of synaptic connectivity and task output in recurrent networks is not enough to predict the activity of single neurons\"; their remedy is recordings from a subset of neurons (error strongly reduced above M > 30 of 300, scaling with the dimensionality of the dynamics). Roadmap 6's two named objectives are exactly task output (\"looming, object tracking\") or \"a fit to whole-brain data (DANDI 000727)\" — and 000727 is 9 subjects at 1.8 Hz voxel-level with no cell identity (research_notes/Коннектом мухи и план проекта/fly_sleep_spontaneous_activity.md:58, :63, :75) against a model timestep of 0.02 s (config.toml:34), i.e. a Nyquist ceiling of 0.9 Hz against a 50 Hz model. It cannot supply the neuron-resolved recordings the theory requires.",
 "why_it_failed_or_held": "Attack (a), scale, failed cleanly and I report it as a failure. The verified base measurement is graph memory = n_edges x 4 B per frame per sample, measured 5.7-6.5 MB converging on 6.05 MB at n_edges = 1,513,231 (research_notes/decoder_stack/inversion.md:186). Body-level edges at w>=5 among typed brain neurons measured 4,636,042 for the whole brain (3.06x FlyVis) and 2,729,796 for the LC-plus-glomeruli scope (1.80x), so 200 frames at batch 4 costs 16.98 GB and 10.00 GB peak respectively — one A100-40GB, and 4.00 GB with the 25-frame gradient checkpointing already verified gradient-identical to 8e-8 relative (inversion.md:186, :189). Even at w>=1 (21,551,326 edges, the 25.6M-connection figure of reports/Коннектом мухи и план проекта.md:21) it is 78.93 GB plain and 18.59 GB checkpointed. Compute scales 1.8-3.1x per iteration on a bandwidth-bound gather/scatter_add, against FlyVis's reference budget of 250k iterations at batch 4 x 50 members (research_notes/Коннектом мухи и план проекта/flyvis_and_simulators.md:140). The model class is also extensible in principle: flyvis exposes register_connectome and a Connectome Protocol (.venv/Lib/site-packages/flyvis/connectome/connectome.py:48, :71), and single-cell nodes already exist (add_single_node:357; flydream/data/export.py:34 uses CT1 that way). So \"tractable in the same differentiable model class\" is not refuted on arithmetic. It is refuted as written on identifiability and validation. What held the claim up: the step can still deliver a number beside a control for a narrow subset — roughly 20 of the 48 LC/LPLC types in MaleCNS have published type-level tuning (Wu et al. 2016, eLife 5:e21022, 22 LC types with behavioural programs; Klapoetke et al. 2017 looming/small-object calcium tuning), and the central-complex head-direction types (EPG 50 cells, PEN_a/PEN_b 42, PEG 18, Delta7 42, ER 26 types/282 cells, measured here) carry a published attractor phenotype. That subset is why this is COSTLY and not FATAL. What does not survive is roadmap 6's deliverable line: \"the map continued into the central brain; the modulation sweeps\". Three specific reasons. (1) The central complex cannot be attached to vision at this scope: at w>=5 there are 0 optic-lobe-to-CX edges, 308 VPN-to-CX and 179 VCN-to-CX, against 22,159 from the rest of the central brain — 97.9% of the CX's extrinsic input comes from the 6,313 cb_intrinsic types the roadmap's \"then the central complex\" phrasing does not include. (2) The map's own measured trajectory argues the continuation lands in the control floor: the completed Sintel run (data/decode/2026-09-18_decode_sintel_flyvis/by_stage.csv, 189 samples, 65 types, 624 s — verified present and complete, 453 rows, contrary to the task's \"may be partial\") gives photoreceptors PixCorr 1.000 on a uniform sample-shuffle floor of 0.1175, but the T4/T5 stage that feeds the lobula columnar neurons has median 0.366 with a gap over its floor of only 0.249, and T5a 0.188, T5d 0.237, T4b 0.249 sit 0.07-0.13 above the floor, with Tm20 (0.1268 vs floor 0.1201) and Mi2 (0.1258 vs 0.1170) already indistinguishable from it. LC/LPLC neurons are one to two synapses further on and pool across the eye, so under AGENTS.md's \"a result is a number beside its control\" the honest expected outcome is a null — publishable as a null, but not the deliverable the roadmap promises. (3) The tracing substrate is the ISS-0002 failure mode brain-wide: only 7 of 31,280 typed cb_intrinsic bodies carry statusLabel \"Reviewed\" (0.02%), the rest being 20,787 \"Roughly traced\" and 10,452 \"Prelim Roughly traced\"; visual_projection is 51.5%/48.5% RT/PRT with 0.0% Reviewed; CX is 70.8%/29.2% with 0% Reviewed — against 34.9% Reviewed (33,314 bodies) in the optic lobe. ISSUES.md:37-61 records what that status did to one giant neuron: CT1 (bodyId 10157, \"Roughly traced\") has 56,517 outgoing rows and 8 reaching a typed right-lobe neuron, losing every CT1-to-T4/T5 pathway FlyVis carries. Every central-brain neuron is a large, individually identified, multi-compartment cell of exactly that kind. On (d): Shiu et al., Nature 634:210-219 (2024) is the comparison that hurts most, in the opposite direction from the one the project assumes. They ran 127,400 FlyWire v630 neurons and 50M synapses in Brian2 LIF on one free parameter — 0.275 mV per synapse, with V_rest -52 mV, threshold -45 mV, tau_m ~11 ms, refractory 2.2 ms, delay 1.8 ms, alpha-synapse tau 5 ms all fixed from prior electrophysiology — and validated it against optogenetics: \"11 are predicted to activate MN9; notably, 10 of 11 of these cell types actually do elicit rostrum extension\", >90% of experimentally tested predictions (research_notes/Коннектом мухи и план проекта/flyvis_and_simulators.md:99-101, sourced to PMC11446845). Their validation target is a causal behavioural readout, not an activity trace, which is why it exists for the central brain at all. So in the central brain the spiking model is more connectome-constrained than the project's rate model by five orders of magnitude in free parameters (1 vs 95,265-311,134), and the project's DECISIONS.md:76-95 justification for the rate class — \"every community project that stitched a graded FlyVis front end onto a spiking MaleCNS brain reports losing direction selectivity at the seam\" — is an argument against the seam, not for 10^5 free parameters. The rate class's genuine advantage is real and load-bearing (end-to-end differentiability, which encoder inversion needs, verified working on stock flyvis at inversion.md:5-52), but it buys inversion at the cost of identifiability, and Shiu's own stated limitations — no gap junctions, no non-spiking neurons, no internal state, no long-range neuropeptides, neuromodulated circuits \"will be poorly modelled\" — apply verbatim to the rate model, while roadmap 6's entire second half is internal state and neuromodulation (\"octopamine gain on Mi1/Tm3/Mi4/Mi9 and T4/T5, R5 slow-wave gating on EPG, mean luminance\", ROADMAP.md:139-141). Neither class has a volume-transmission mechanism and the connectome carries no octopamine gain, so those knobs are free hyperparameters wearing a connectome's clothes. One knob does check out against the data: ER5 exists in MaleCNS and makes 2,012 synapses onto EPG at w>=5 (measured), so R5-to-EPG gating is at least wireable. Finally, an engineering cost the roadmap does not name: flyvis ships exactly one connectome implementation, ConnectomeFromAvgFilters (connectome/connectome.py:109), which builds nodes by tiling or striding over a hexagonal extent and edges by add_conv_edges (:473) over hex offsets plus fill_hull (:435), and flydream/model/zero.py:62-69 and flydream/data/export.py both commit to that shape. A non-columnar central brain has no lattice and no offsets, so a new registered Connectome over a per-neuron edge list is required, and with it new paths for the transplant, for LayerActivity/stimulus_response indexing by (type, column), and for flydream/decode/hexraster.py.",
 "required_changes": [
  "Cut the scope from 'VPNs and their targets' to the feedforward bottleneck only. Measured: adding the 48 LC/LPLC types alone costs 17,184 free parameters (23.4x FlyVis) and 2,386,909 edges; adding their direct central-brain targets costs 95,265 (129.8x) because those targets are 3,608 of 6,313 non-CX cb_intrinsic types. Build the LC/LPLC layer as a readout over a frozen transplanted optic lobe, not a jointly trained central brain.",
  "Move 'then the central complex' behind its own human gate and drop it from item 6. At w>=5 the CX receives 0 synapses from the optic lobe, 308 from VPNs and 179 from VCNs against 22,159 from the rest of the central brain: 97.9% of its extrinsic drive is outside the scope, so a visually driven CX in this scope is not a model of the CX.",
  "Replace 'Training objective: a behavioural task (looming, object tracking) or a fit to whole-brain data (DANDI 000727)' with a fit to published type-level tuning on the ~20 of 48 LC/LPLC types that have it (Wu et al. 2016 eLife 5:e21022; Klapoetke et al. 2017), and demote DANDI 000727 to a state/modulation prior. At 1.8 Hz voxel-level over 9 subjects it cannot constrain a 50 Hz per-cell-type model, and Beiran & Litwin-Kumar 2025 says task output alone will not either.",
  "Report the ensemble spread as the primary result, not as a secondary control. With >=99.3% of parameters median-initialised and 10^5 of them free, the spread across members is the measurement of degeneracy that Beiran & Litwin-Kumar predict; a single-member central-brain figure would be uninterpretable.",
  "Budget a new flyvis Connectome class registered via register_connectome over a per-neuron edge list, plus replacements for the type-pair transplant, the (type, column) activity indexing and the hex raster. ConnectomeFromAvgFilters can only tile hex-offset convolutional filters and cannot represent a non-columnar network.",
  "Name the state knobs as free hyperparameters with no connectome constraint behind them, and say so in the report. Octopamine gain and mean-luminance gain have no synapse count; the connectome carries chemical synapses only, and both FlyVis and Shiu et al. explicitly exclude neuromodulation.",
  "Add a stability gate before any priced run: the transplant's shuffled-strength control already diverged 3/3 (reports/runs.jsonl, step2.control) and central-brain cells carry 812 input synapses each against the optic lobe's 269, so the median-filled central-brain coefficients must be shown bounded on a short local run before Modal time is bought.",
  "State the expected outcome of the map continuation as a null before running it. At the deepest measured stage the gap over the sample-shuffle floor is 0.249 (T4/T5 median 0.366 vs floor 0.1175) with three subtypes within 0.13 of the floor, so 'the map continued into the central brain' should be pre-registered as a floor test, not promised as a curve.",
  "Record a tracing-quality caveat per central-brain figure: 7 of 31,280 typed cb_intrinsic bodies are 'Reviewed' (0.02%) against 34.9% in the optic lobe, and ISS-0002 shows what that status cost one giant neuron."
 ],
 "numbers": [
  "MaleCNS central brain (cb_intrinsic + visual_projection + visual_centrifugal + cb_sensory + DN/AN, typed): 49,667 cells in 8,351 types, median 2 cells per type, mean 5.9; 53.2% of types have <=2 cells, 73.8% have <=4. Optic lobe: 95,455 cells in 282 types, median 69, mean 338.5. Measured from data/malecns/body-annotations-male-cns-v1.0-minconf-0.5.feather.",
  "Free parameters under the project's own model class (2 per node type + 1 per type pair, the formula that gives FlyVis 734 = 2x65 + 604, reports/Коннектом мухи и план проекта.md:38), at config.toml:7's min_weight = 5, measured from the 151,856,684-row weights table: OL at full 282-type resolution 13,675; OL + 48 LC/LPLC types 17,184; OL + LC/LPLC + their 1,529 direct central-brain target types 95,265 (129.8x FlyVis); OL + all VPN + VCN + CX 46,897; OL + all VPN + VCN + all glomerular targets + CX 311,134; whole brain 529,921 (722x). At w>=1 the whole brain is 2,791,654.",
  "Parameter-to-edge ratio: FlyVis 734 parameters over 1,513,231 edges = 1 per 2,062. OL at full typing 1 per 154.5. OL + LC/LPLC + targets 1 per 28.7. Whole brain 1 per 8.7 — a 237x degradation of the constraint.",
  "Type-pair pooling at w>=5: central-brain type pairs pool a mean of 4.67 body-to-body edges, median 2, with 38.9% pooling exactly one, 64.1% <=2, 84.6% <=4. Optic-lobe type pairs pool a mean of 161.15, median 5, 26.2% exactly one. At w>=1: central brain mean 4.62 with 48.1% pooling exactly one; optic lobe mean 248.69.",
  "Scope of 'VPNs and their targets': the 346 visual_projection types' direct postsynaptic partners at w>=5 span 3,608 of the 6,313 non-CX cb_intrinsic types (57.2%); the 48 LC/LPLC types alone reach 1,529 (24.2%). Corroborated independently by bioRxiv 2026.02.02.700492, 'Connectome analysis reveals brainwide visual processing in Drosophila': visual inputs reach 'more than half of central-brain cell types'.",
  "Central complex isolation at w>=5 (measured body-level edges): into CX from optic lobe 0, from VPN 308, from VCN 179, from non-CX central brain 22,159, intra-CX 138,641. 97.9% of extrinsic CX input comes from outside roadmap 6's stated scope. CX itself: 2,950 cells in 292 types (EPG 50, PEN_a/PEN_b 42, PEG 18, Delta7 42, EL 18, PFL 50, PFN 456, ER 282 across 26 types, ExR 26, FB 575 across 160 types).",
  "Autograd-graph arithmetic from the verified base (research_notes/decoder_stack/inversion.md:186, graph memory = n_edges x 4 B per frame per sample, measured 5.7-6.5 MB converging on 6.05 MB at 1,513,231 edges): body-level edges at w>=5 among typed brain neurons are 2,112,814 (OL), 2,729,796 (OL+LC/LPLC+targets), 3,024,681 (OL+VPN+VCN+CX), 4,636,042 (whole brain), 21,551,326 (whole brain w>=1) — i.e. 8.45 / 10.92 / 12.10 / 18.54 / 86.21 MB per frame per sample. At 200 frames x batch 4 plus resident network: 7.74 / 10.00 / 11.08 / 16.98 / 78.93 GB, and 1.82 / 2.35 / 2.61 / 4.00 / 18.59 GB with the 25-frame gradient checkpointing verified gradient-identical to 8e-8 relative (inversion.md:186, :189). The scale attack fails: the whole brain fits one A100-40GB.",
  "Per-iteration compute scaling (bandwidth-bound gather + scatter_add, inversion.md:185): 1.80x FlyVis at the LC-plus-glomeruli scope, 2.00x at OL+VPN+VCN+CX, 3.06x at whole brain w>=5, 14.2x at w>=1. Reference budget: FlyVis 250,000 iterations at batch 4, lr 5e-5 to 5e-6, 50-member ensemble (research_notes/Коннектом мухи и план проекта/flyvis_and_simulators.md:140, PMC11525180); measured CPU baseline 0.5 s per iteration at 50 frames batch 1 on 16 threads, 3.0 s at 200 frames (inversion.md:185).",
  "Input drive mismatch: central-brain cells receive 812.2 input synapses each on average (median 339, p90 1,752) at w>=5 against the optic lobe's 269.4 (median 156, p90 409) — 3.0x the mean, 2.2x the median; per-type mean input synapses per cell, median over types, 704 for the central brain against 328 for the optic lobe. Since the model's current is sign x n_synapses x per-type-pair coefficient, median-filled coefficients deliver ~2-3x the drive the transplant was tuned for.",
  "Transplant coverage: FlyVis supplies 604 type pairs, all optic-lobe intrinsic (reports/Коннектом мухи и план проекта.md:38-40), and even its own bridge covers 61 of 65 nodes and 496 of 571 pairs (ROADMAP.md:31, :78-81; reports/runs.jsonl step1.filters type_pairs_recovered 0.869). So at the LC-plus-glomeruli scope >=90,943 of 91,547 type pairs (99.34%) would start at a FlyVis median — while reports/runs.jsonl step2.control records 'shuffled_strengths_diverged' value 1.0, n 3: permuting the already-fitted strengths on the existing wiring ran away to inf in every member under both protocols.",
  "Tracing status, measured: typed cb_intrinsic bodies 31,280 — Roughly traced 20,787 (66.5%), Prelim Roughly traced 10,452 (33.4%), Reviewed 7 (0.02%). visual_projection 9,201 — 51.5% RT, 48.5% PRT, 0.0% Reviewed. CX 2,950 — 70.8% RT, 29.2% PRT, 0% Reviewed. Optic lobe 95,455 — 34.9% Reviewed (33,314 bodies). ISSUES.md:37-61: CT1 bodyId 10157, statusLabel 'Roughly traced', 56,517 outgoing weight rows and 8 reaching a typed right-lobe neuron.",
  "Synapse loss to untyped bodies at w>=5: 77.5% of edges out of typed central-brain neurons reach a typed target (90.2% of synapses), against 88.6% of edges / 93.7% of synapses in the optic lobe. Of 7,622,864 whole-CNS weight rows at w>=5, 5,121,971 (67.2%) have both endpoints typed brain neurons.",
  "Recurrence, measured on the type-level graph at w>=5: optic lobe 282 types / 13,111 type-pair edges, largest strongly connected component 281 (99.6%), 56.7% of edges reciprocal, mean type in-degree 46.5; central brain 7,217 types / 495,460 edges, largest SCC 7,207 (99.9%), 22.5% reciprocal, mean in-degree 68.7. The optic lobe is already almost fully recurrent at type level, so 'the central brain is more recurrent' is a weaker attack than it looks and I do not rest on it.",
  "Sintel decodability map, verified present and complete (data/decode/2026-09-18_decode_sintel_flyvis/map.csv, 453 rows, 65 types, 7 controls; meta.json: 189 samples, 40 frames, 624 s, flow/0000/000): photoreceptors real PixCorr 1.000 (R1 0.9999, R5 0.9995, R6 0.9992, R7 0.928), sample-shuffle floor uniform at 0.1175 median (range 0.0969-0.1239), time-shuffle 0.1184-0.5871 — all three task premises confirmed. Stage medians (real / time-shuffle / floor): photoreceptor 1.000/0.421/0.118, lamina 0.712/0.248/0.117, medulla Mi 0.666/0.145/0.117, medulla Tm/TmY 0.652/0.144/0.117, T4/T5 motion 0.366/0.131/0.117, lobula/other 0.734/0.158/0.116. T4/T5 gap over floor 0.249, with T5a 0.1878, T5d 0.2371, T4b 0.2492; Tm20 0.1268 vs floor 0.1201 and Mi2 0.1258 vs 0.1170 are already at the floor. Note: these Sintel rows are not yet in reports/runs.jsonl, whose last entries are the moving-edge run.",
  "Shiu et al., Nature 634:210-219 (2024): 127,400 FlyWire v630 neurons, 50M synapses, Brian2 LIF, one free parameter (0.275 mV per synapse) with V_rest -52 mV, threshold -45 mV, reset -52 mV, tau_m ~11 ms, refractory 2.2 ms, delay 1.8 ms, alpha-synapse tau 5 ms fixed from prior electrophysiology; ~5 CPU-minutes per 1,000 ms trial per thread. Validated against optogenetics: '11 are predicted to activate MN9; notably, 10 of 11 of these cell types actually do elicit rostrum extension', >90% of experimentally tested predictions. Stated limitations: no gap junctions, no non-spiking neurons, no internal state, no long-range neuropeptides; zero basal firing so inhibition onto a silent neuron has no effect; neuromodulated circuits 'will be poorly modelled' (research_notes/Коннектом мухи и план проекта/flyvis_and_simulators.md:99-101, sourced to PMC11446845). Ratio of free parameters to this project's central-brain scope: 1 vs 95,265-311,134.",
  "Beiran & Litwin-Kumar, Nature Neuroscience 28:2561-2574 (2025), 'Prediction of neural activity in connectome-constrained recurrent networks': 'Knowledge of synaptic connectivity and task output in recurrent networks is not enough to predict the activity of single neurons'; connectome-constrained networks have O(N) unknown parameters against N^2 unconstrained; error strongly reduced when recording from M > 30 neurons in a 300-neuron network (~10%), with the required count scaling with the dimensionality of the dynamics rather than network size (rank-2 networks needed ~7; chaotic networks more); tested at N = 100-1600 and N = 1000.",
  "Validation target resolution gap: DANDI 000727 is 609.6 GB, 18 NWB files, 9 subjects, two-photon volumetric at 1.8 Hz voxel-level with no cell-type identity, described by the project's own notes as 'coarse (1.8 Hz, voxel-level) resolution' (research_notes/Коннектом мухи и план проекта/fly_sleep_spontaneous_activity.md:58, :63, :75). The model timestep is 0.02 s = 50 Hz (config.toml:34). Nyquist ceiling 0.9 Hz against a 50 Hz model, 28x slower than the integration step.",
  "State knob wireability, measured: ER5 exists in MaleCNS and makes 2,012 synapses onto EPG at w>=5 (its larger targets are ER5 itself 15,478, EL 10,073, ExR1 10,044). So R5-to-EPG gating is wireable; the octopamine gain is not, because no chemical-synapse count in the connectome represents volume transmission.",
  "Model-class extensibility: flyvis ships exactly one connectome implementation, ConnectomeFromAvgFilters (.venv/Lib/site-packages/flyvis/connectome/connectome.py:109), building nodes by add_tiled_nodes/add_strided_nodes/add_single_node over a hex extent and edges by add_conv_edges (:473) plus fill_hull (:435). A Connectome Protocol (:48) and register_connectome (:71) exist, so a per-neuron class is registrable — but flydream/model/zero.py:62-69 and flydream/data/export.py both commit to the tiled hex-offset shape, as does flydream/decode/hexraster.py."
 ],
 "unchecked": [
  "No GPU on this machine, so every wall-clock figure is derived from the 16-thread CPU measurement at inversion.md:185, which itself says 'the wall-clock ratio on a GPU is unmeasured'. The 1.8-3.1x compute scaling assumes the per-step cost stays proportional to n_edges on GPU; I did not verify it, and a per-neuron central-brain edge list loses the convolutional weight sharing that makes the optic-lobe gather cache-friendly, so the real ratio could be worse.",
  "I could not retrieve the published Beiran & Litwin-Kumar paper (nature.com 303-redirects to an IdP; the Columbia PDF mirror fails TLS hostname validation). The quantitative claims come from the bioRxiv v1 full text via WebFetch, so page/figure numbers in the Nature Neuroscience version are unverified.",
  "I could not fetch the Shiu et al. primary text (biorxiv HTTP 429, and the PMC id I tried resolved to an unrelated paper). Every Shiu number is taken from the project's own research note, which cites PMC11446845; I verified the shape of the claims against a web search summary but not the parameter table against the paper itself.",
  "I could not fetch bioRxiv 2026.02.02.700492 ('Connectome analysis reveals brainwide visual processing in Drosophila', HTTP 429) or the Cell version of 'The organization of visual pathways in the Drosophila brain' (HTTP 403). The 'more than half of central-brain cell types' corroboration is from a search snippet only; the bottleneck size, the feedback/bilateral class counts, and which connectome version they used are unverified, and that paper is likely the closest prior art to roadmap 6.",
  "My '~20 of 48 LC/LPLC types have published type-level tuning' is an estimate from the Wu et al. 2016 / Klapoetke et al. 2017 abstracts and search summaries, not a name-by-name audit against the 48 MaleCNS LC/LPLC type names. I also did not check the sex of the animals in those recordings, nor in FlyVis's 26 validation studies, so I make no claim about a male/female mismatch.",
  "I partitioned the connectome by the MaleCNS 'superclass' field. 44,877 of 211,577 bodies have no superclass and 47,071 have no type; both are excluded from every count above, which is itself the ISS-0002 error mode and means my edge and type-pair counts are lower bounds on the real model size, not upper bounds.",
  "I did not model what the project's density-stride rule (reports/2026-09-18_step2_model_zero.md §3.2, stride_for_density) would do to a central-brain scope. It has no meaning for cells that are not tiled over columns, so the node and edge counts a registered per-neuron class would actually produce are unmeasured.",
  "I did not test stability or BPTT quality of a rate model at central-brain scale — neither the divergence risk from median-filled coefficients nor gradient behaviour over the 500-Euler-step horizons roadmap 7 implies. inversion.md:187 states the long-horizon question was untested there too ('Gradient norms were stable and finite at 8/25/50/100/200 frames' but no convergence experiment).",
  "Thresholds other than w>=5 and w>=1 were not swept, only the right-hemisphere-plus-central asymmetry was ignored (I did not split by side), and I did not check the left optic lobe, which ROADMAP.md:191 lists as not exported.",
  "ROADMAP.md:5 still says 'idea and research done, no code yet' while ROADMAP.md:13 and the Done list record steps 1, 2 and part of 4 as built and reported. I noted the contradiction but did not resolve it, and it is a records defect rather than evidence about this leg."
 ]
}
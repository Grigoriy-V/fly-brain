{
 "leg": "Training the MaleCNS substrate to FlyVis-level validation on Modal, at a single researcher's cost",
 "verdict": "COSTLY",
 "headline": "Modal and money are not the constraint (~$110-900 buys the whole 50-model ensemble); the constraint is CT1: deleting CT1 to T4/T5 from FlyVis's own trained network costs T5 91% of its direction selectivity (0.587 to 0.052), and the MaleCNS export has no CT1 edges at all, while sign and synapse count are non-trainable, so training cannot invent it — meanwhile 6.4x of the reported 20x DSI collapse is a parameter-scaling bug in step 2's transplant, not a property of the connectome.",
 "strongest_attack": "I ablated CT1 inside FlyVis itself (member flow/0000/000, MovingEdge 12 angles x 2 intensities, speed 13, dt 0.02, no retraining) by zeroing the 8 syn_strength pairs CT1(M10)->T4a-d and CT1(Lo1)->T5a-d. Mean T4/T5 DSI falls 0.505 -> 0.188; split by family, T4 0.423 -> 0.325 (-23%) and T5 0.587 -> 0.0515 (-91%); PD-within-45deg falls 1.000 -> 0.750. Deleting ALL 27 pathways the MaleCNS export lacks (CT1, Mi10, C2, T4<->T5 cross-links, TmY4->T5d) adds almost nothing beyond CT1 alone: 0.167 / T4 0.284 / T5 0.050. So CT1 is essentially the entire deficit. In data/ol/filters_R.json the four T5 subtypes have exactly ONE inhibitory input type left (TmY15, 20-23 offsets, wide-field), because CT1(Lo1) supplies 18.0-21.9% of T5's input synapses per column in FlyVis and 28.5-33.5% of the realized |weight| onto its central cell. Gruntman, Romani & Reiser (eLife 2019;8:e50706) conclude T5 direction selectivity is null-direction suppression by \"fast depolarization and persistent, spatially offset hyperpolarization\", and name CT1 as the local inhibitory source. Training cannot supply it: in data/flyvis/results/flow/0000/000/_meta.yaml the sign parameter is requires_grad: false (line 37) and syn_count is requires_grad: false (line 45); only syn_strength is trainable (line 55), and a pathway with zero edges has no gain to train. ISSUES.md:30/37-61 (ISS-0002) records why the edges are missing: body 10157 has 56,517 outgoing weight rows and 8 that reach a typed right-lobe neuron.",
 "why_it_failed_or_held": "It is not FATAL because the headline evidence for \"the substrate is broken\" is largely a bug in the transplant, and I measured the repair. FlyVis parameterises syn_strength as rho/<N>_pair (flyvis/network/initialization.py:501, \"syn_strength = scale / grouped_edges.n_syn.values\"), so an edge weight is rho*m*N(offset)/<N>_pair. flydream/model/zero.py:80-110 copies the raw strength by type-pair key and ignores that MaleCNS's <N>_pair differs, which multiplies every weight of a pair by <N>_mc/<N>_fv. Over the 395 matched pairs that factor has median 1.847, p5 0.606, p95 7.156, max 34.18; Tm9->T5a-d (T5's largest excitatory driver, 47.9% of realized input) is underweighted 3.2-4.2x. The result in the saved run is an operating-point failure, not a weak response: in data/runs/2026-09-18_step2_zero_R_v5/edge_malecns_000.pkl, T4a rests at V=3.505 with peak 3.603 (modulation depth 0.142 against FlyVis's 2.57), Mi1's transmitted range is 0.014 on a mean of 2.132 (0.7%), and Tm9 sits at V in [-2.49, -0.54] so relu(V) is identically 0 in both protocols — T5's main driver is switched off. Applying the scale-free correction (strength x <N>_fv/<N>_mc) with NO training lifts mean T4/T5 DSI from 0.025 to 0.159 at the identical protocol setting, with T4c 0.778 (above FlyVis 000's own 0.573), PD error 11.5deg -> 7.4deg and PD-within-45deg 1.000. That corrected figure (0.159, T4 0.312) lands on top of FlyVis-with-the-missing-pathways-deleted (0.167, T4 0.284) — agreement within 5% overall and 10% on T4. So the substrate behaves exactly like FlyVis minus CT1, no worse. Attack (b) also fails: the shuffled-strength control's divergence is not evidence of an edge of stability, it is a control built far outside the stable set — its all-active Jacobian has max Re eig +48.1 and spectral radius of W = 3.52, against the transplant's -3.16 / 0.632, FlyVis-trained's -3.06 / 1.760 and a fresh MaleCNS init's -15.83 / 0.208. Attack (c) fails on arithmetic: the model is 0.55x FlyVis's edge count, so the whole 50-model ensemble prices at roughly $109-875 on Modal A100-40GB. What survives unrefuted, and what makes this COSTLY rather than SURVIVES, is that \"FlyVis-level validation\" includes T5 direction selectivity, and nothing in the training step can create the pathway the reference literature says produces it.",
 "required_changes": [
  "Fix the transplant/initialisation before any priced run. Either train from FlyVis's own connectome-adaptive init (syn_strength = 0.01/<N>_malecns, flyvis/network/initialization.py:501) or rescale the transplanted strengths by <N>_fv/<N>_mc (median 1.847, up to 34.18x). Measured with zero training: mean T4/T5 DSI 0.025 -> 0.159, T4c 0.778, PD error 11.5deg -> 7.4deg. Launching a 250k-iteration run from the as-built transplant starts it at an operating point where Tm9 is relu-silent.",
  "Check stability of whichever start is chosen with a short run first: the scale-corrected network's all-active Jacobian has max Re eig +2.76 and excitatory row sums up to 17.0 (the as-built transplant: -3.16 and 6.97), so the correction trades a wrong operating point for a possibly unstable one. FlyVis's own trained multipliers reach 417x its initialisation with median 4.95, i.e. training itself walks the network from spectral radius 0.21 to 1.76 and the activity penalty (penalizer.yaml activity_baseline 5.0, above_baseline weight 0.1, stop_iter 150000) is what holds it. A diverged run raises OverflowError (flyvis/solver.py:373-375) and resumes only from the last checkpoint, every 300 epochs (scheduler.yaml chkpt_every_epoch: 300, about 5,400 iterations), so budget a retry allowance.",
  "Either restore CT1 (ISS-0002) before the training run, or remove T5 direction selectivity from step 3's validation target and say so in the report. This is a data step, not a training knob: neuPrint partner queries on unproofread fragments at 42% postsynaptic completion, with the result checked, not a synthetic CT1 invented by rule.",
  "Buy one control model before the ensemble: retrain FlyVis on FlyVis's own connectome with CT1->T4/T5 deleted, 250k iterations, and measure whether T5 DSI recovers from 0.052. One model costs $2-18 and settles whether step 3 can reach its stated target at all; the ensemble is only worth buying if it does.",
  "Size the ensemble at 20-50 members, not 3. FlyVis's own members 000/001/002 span DSI 0.281-0.547 and PD-within-45deg 0.625-1.000 (reports/2026-09-18_step2_model_zero.md:46), so a 3-member MaleCNS result cannot demonstrate FlyVis-level validation whatever it scores.",
  "Pack several models per Modal container. One 829k-edge model at batch 4 and 19 sequential Euler steps cannot fill an A100; at one model per container the ensemble bill is multiplied by the packing factor that is being thrown away.",
  "Add a GPU per-iteration benchmark as an explicit step-3 line item. The reference reports no compute at all (research_notes/Коннектом мухи и план проекта/flyvis_and_simulators.md:140 and :153), so every dollar figure for this step is an extrapolation until one iteration is timed on the target GPU.",
  "Correct the step-2 record: reports/2026-09-18_step2_model_zero.md:68 lists the candidates for the lost selectivity as missing CT1, median parameters for 56 pairs, filter-scale mismatch on wide-field TmY types and four absent types. The measured decomposition is different: a parameter-scaling error worth 6.4x, plus CT1. The 56 defaulted pairs and the TmY types are not implicated by these numbers."
 ],
 "numbers": [
  "FlyVis member 000, MovingEdge 12 angles x 2 intensities, speed 13, dt 0.02, no retraining — mean T4/T5 DSI: baseline 0.5048 (T4 0.423, T5 0.587, PD-within-45deg 1.000); CT1->T4/T5 deleted 0.1884 (T4 0.325, T5 0.0515, within-45deg 0.750); all 27 pathways the MaleCNS export lacks deleted 0.1668 (T4 0.284, T5 0.050). My run, scratchpad/ablate.py + ablation.json.",
  "Same protocol, MaleCNS transplant as built: mean T4/T5 DSI 0.0250 (T4 0.0355, T5 0.0145). Scale-corrected transplant (strength x <N>_fv/<N>_mc): 0.1590 (T4 0.312, T5 0.0058), PD error 7.4deg, PD-within-45deg 1.000. My runs, scratchpad/raw_match.json and rescale.json.",
  "Scale-correction factor <N>_fv/<N>_mc over 395 of 451 matched pairs: median 1.847, p5 0.606, p95 7.156, min 0.144, max 34.18; Tm9->T5a x4.15, T5b x3.35, T5c x3.17, T5d x3.85; TmY15->T5a-d x0.43-0.63. My run, scratchpad/rescale.py.",
  "CT1's share of T4/T5 input in FlyVis's fib25-fib19_v2.2.json, synapses per column summed over offsets: CT1(M10) 7.8-11.8% of T4a-d total input; CT1(Lo1) 16.7-21.9% of T5a-d total input. Realized |weight| onto the central cell after strides: CT1(Lo1) is 33.5% of T5a's and 28.5% of T5c's input, the largest single term in both. My run, scratchpad/t4t5.py + audit_net.py.",
  "Realized inhibition onto the central cell, FlyVis 000 vs MaleCNS transplant: T5a 1.891 vs 0.146 (13x loss), T5c 2.002 vs 0.140 (14x), T4a 1.930 vs 0.688 (2.8x). Net drive T5c +0.394 vs +1.879. In data/ol/filters_R.json the only inhibitory (alpha=-1) source left on T5a-d is TmY15. My run, scratchpad/audit_net.py.",
  "Operating point in the saved step-2 run (data/runs/2026-09-18_step2_zero_R_v5/edge_malecns_000.pkl vs edge_flyvis_000.pkl): network-wide resting mean V 1.525 vs 0.406, max |V| 16.9 vs 6.5; T4a rest 3.505 with peak 3.603 (modulation depth 0.142) vs rest 0.024 with peak 0.406 (depth 2.57); Mi1 transmitted range 0.014 on mean 2.132 (0.7%); Tm9 V in [-2.49,-0.54] so relu(V) = 0 throughout. My run, scratchpad/traces.py + lam.py.",
  "Stability, max Re eigenvalue of the all-active Jacobian diag(1/tau)(W-I) and spectral radius of W: FlyVis trained 000 -3.065 / 1.760; MaleCNS fresh init -15.833 / 0.208; MaleCNS transplant -3.162 / 0.632; MaleCNS transplant rescaled +2.762 / (excitatory-only +1.326); MaleCNS shuffled-strength control +48.078 / 3.517 with row |w| sums to 14.45. The control is outside the stable set by construction. My run, scratchpad/audit_net.py + margin.py.",
  "FlyVis's trained multiplier m = syn_strength x <N> / 0.01 over 604 pairs: min 0, p5 0, median 4.951, p95 39.549, max 417.333; 64.2% above 2x, 26.2% below 0.5x, 5.1% driven to exactly zero. Training moves gains over three orders of magnitude and prunes pairs, so a 'bad basin' from the transplant is not the leading risk. My run, scratchpad/margin.py.",
  "Model size measured at build: MaleCNS 31,526 nodes / 828,944 edges against FlyVis 45,669 / 1,513,231 — 0.69x nodes and 0.55x edges, so per-iteration compute is BELOW the reference's, not above. Free parameters 571 in both (571 free / 3,063 fixed, logged by flyvis at init).",
  "Modal cost arithmetic. 250,000 iterations (flyvis/config/task/task.yaml:44) x batch 4 (:43) x 19 frames (:9) = 4.75M forward timesteps per model plus backward. The reference reports no GPU hours (research_notes/.../flyvis_and_simulators.md:140, :153), so bracket the per-iteration time at 15/30/60/120 ms: 1.04/2.08/4.17/8.33 h per model. Modal A100-40GB $0.000583/s = $2.0988/h (modal.com/pricing, fetched 2026-09-18) gives $2.19/$4.37/$8.75/$17.49 per model, so 50 models = $109/$219/$437/$875 and 9 models = $20/$39/$79/$157. On L4 at $0.000222/s ($0.7992/h) it is 2.6x cheaper; peak retained activation is about 829k edges x 19 frames x 4 batch x 4 B = 252 MB per tensor, so 24 GB is ample. Cost is one to three orders of magnitude below a blocker even with retries.",
  "Local CPU is confirmed out of the question (consistent with DECISIONS.md:96-110): measured forward+backward at the real training shape on 2 contended threads, 2.68 s/iteration for FlyVis and 2.98 s for MaleCNS, i.e. 186 and 207 CPU-hours for 250k iterations. My run, scratchpad/bench.py.",
  "Flash validation is weaker than the headline. Median |FRI| over the 59 shared types is 0.284 for FlyVis 000 and 0.098 for the MaleCNS transplant (median ratio 0.40), and the number of types with |FRI| < 0.05 rises from 5 to 17 — the reported polarity agreement of 0.911 is a sign test, and for nearly a third of types the sign is being read off a near-zero index. It is also scored on 30 known types against FlyVis's 32. Computed from data/runs/2026-09-18_step2_zero_R_v5/fri_*_000.csv.",
  "The Sintel decodability map finished during this audit (data/decode/2026-09-18_decode_sintel_flyvis/map.csv, meta.json: 189 samples, 40 frames, 65 types, 624 s). The task's stated facts check out: photoreceptors 0.9992-1.000 PixCorr, sample-shuffle floor uniform at 0.1160-0.1239, time-shuffle 0.118-0.587. Note for the leg after this one: on the validated FlyVis model the T4/T5 end of the map is already near the floor (T5a 0.188, T4b 0.249, T5d 0.237 against a floor of 0.117)."
 ],
 "unchecked": [
  "Whether 250k iterations of retraining recovers T5 direction selectivity on a connectome with no CT1. This is the single question that decides FATAL vs COSTLY and it cannot be answered locally — my ablations are un-retrained, which is an upper bound on the damage. The control run named in required_changes is the cheapest way to settle it.",
  "GPU per-iteration time. There is no local GPU and the reference publishes no compute, so every dollar figure rests on my 15-120 ms/iteration bracket rather than a measurement. The CPU benchmark was run on a machine with two other heavy jobs (flyvis mean 2.68 s but min 0.97 s per iteration), so it bounds nothing tightly.",
  "Modal Volume storage pricing was not fetched — only the compute rates. Sintel on disk here is 5.8 GB plus a 61 MB rendering cache, and moving it to a Volume is itself a >1 GB transfer, i.e. a human gate.",
  "Every ablation number is one ensemble member (000), one speed (13), dt 0.02, right lobe, weight>=5. I did not repeat over members 001/002 or over the three speeds the step-2 report used, so the DSI values are not directly comparable to the report's 0.391/0.020 — only the within-setting contrasts are.",
  "The rescaled transplant's flash number (polarity 0.900, fri_corr 0.749) was run at dt 0.02 while the report's 0.967/0.799 was at dt 0.01, so that pair is not a matched comparison.",
  "Whether CT1's edges can actually be recovered from neuPrint partner queries. ISS-0002's stated cause is explicitly unproven, and if the partners are unproofread fragments there may be nothing to recover at v1.0.",
  "I did not isolate the effect of the 56 type pairs that got FlyVis's median strength, nor the four absent types (Mi3, Mi11, Mi12, Tm28) — the scale correction cannot be applied to those 56 pairs, so part of the residual gap may still sit there.",
  "FlyVis's own training success rate is unknown: the paper does not report how many of the 50 runs diverged or were restarted, so I cannot put a probability on a MaleCNS run exploding, only show that the fresh init starts far from the boundary and that training demonstrably walks toward it."
 ]
}
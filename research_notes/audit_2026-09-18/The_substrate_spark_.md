{
 "leg": "The substrate spark — MaleCNS as a fixed recurrent substrate with trained input/readout for image generation or token prediction (ROADMAP.md:166-169; DECISIONS.md:153-155)",
 "verdict": "FATAL",
 "headline": "The LLM half is arithmetically impossible (a frozen fly contributes 0 trainable parameters while its readout alone is 2.295e9 = 18.5x GPT-2 small), and the one peer-reviewed degree-preserving-null test across species found the fruit fly to be the single connectome that does NOT beat its rewired null (p = 0.11), so the leg as written is a stunt whose control the project has neither implemented nor can currently compute.",
 "strongest_attack": "Suárez et al., conn2res, Nature Communications 2024 (open text: https://pmc.ncbi.nlm.nih.gov/articles/PMC10803782/) ran exactly the control ROADMAP.md:168 specifies — connectome reservoirs against 500 rewired nulls that \"preserve network density and degree sequence\" — across five species on memory capacity, and reports verbatim: \"At peak memory capacity, biologically-informed connectomes perform significantly better than rewired nulls, except for the fruit fly\". Fly p = 0.11 at alpha_peak = 0.9 (n.s.); mouse p < 0.002, rat p < 0.002, macaque p < 0.002, human p = 0.002. The fly is the one species that fails. Second barrel: the only paper that ran a degree-preserving null on a real fly connectome at scale (FlyGM, arXiv 2602.17997, Table 1, speed 3 / yaw 7) gets position error 0.0364+-0.002 for the connectome against 0.0370+-0.002 for degree-preserving rewiring — a 1.6% difference with overlapping error bars — while its headline 15.1x gap is entirely against an Erdős–Rényi graph that does NOT match degree. The advertised connectome advantage is a degree-and-weight-distribution effect, not an exact-wiring effect.",
 "why_it_failed_or_held": "Five independent failures, two of them unrecoverable.\n\n(1) LLM half, arithmetic. \"Fixed substrate with trained input and readout\" (ROADMAP.md:167) means 0 trainable parameters in the fly. With GPT-2's vocabulary the readout alone is 45,669 x 50,257 = 2.295e9 params = 18.5x GPT-2 small (124M) and 69.6x TinyStories-33M; on model zero as actually built (31,526 neurons, ROADMAP.md:36-37) it is 1.584e9 = 12.8x GPT-2 small. The input embedding into the substrate's 721-hexal port (config.toml:11-12) is 50,257 x 721 = 36.2M, itself larger than TinyStories-33M. Even the most charitable byte-vocab configuration leaves an 11.88M-parameter trained wrapper around a 0-parameter fly. TinyStories (Eldan & Li 2023) puts the floor for coherent English at ~1-28M params, so in every configuration 100% of the learnable capacity sits in the wrapper and the wrapper alone is a language model. The prior art already demonstrates this: nftechie/FLM is a 278k adapter feeding MaleCNS activity into a Liquid AI 1.2B LM (reports/Коннектом мухи и план проекта.md:165) — the fly is 0.023% of the parameters and the 1.2B model does the language.\n\n(2) The substrate cannot hold linguistic context. FlyVis time constants initialise at 50 ms (research_notes/Коннектом мухи и план проекта/flyvis_and_simulators.md:19) at dt = 20 ms (config.toml:34): per-step retention exp(-0.4) = 0.670, falling below 1% after 12 steps = 0.24 s. Dambre et al. 2012 (Sci Rep 2:514) bounds total information-processing capacity by the number of linearly independent state variables under fading memory — the ceiling is N <= 31,526, but the realised horizon is ~10 timesteps against the O(1e3) tokens token prediction needs, a 100x shortfall.\n\n(3) The project's control does not exist and is inverted. ROADMAP.md:168 requires \"a shuffled-connectivity control of the same size\". No wiring shuffle exists in the code; the only shuffle is flydream/model/zero.py:72-78 shuffle_strengths, which permutes per-type-pair synapse strength parameters and leaves the graph untouched — the exact opposite of what a substrate control needs (scramble wiring, hold gain). And it diverged to inf in 3/3 members under both protocols (reports/runs.jsonl:16; step2 report §2.1-2.2), so it cannot produce a comparable number at all. Worse, \"same size\" is not \"same degree distribution\": per FlyGM's Table 1 above, a size-matched-but-not-degree-matched shuffle manufactures a large gap that measures degree and weight statistics. The experiment as designed is built to succeed for the wrong reason — a false positive by construction.\n\n(4) The substrate is detuned away from the only regime where any advantage exists. conn2res finds the connectome effect only near criticality (alpha_peak 0.9-1.15); the Drosophila-ESN study (PMC12109256) states \"in the case of a spectral radius of 0.25, the classical ESN consistently performs better than the connectome-based ESN\" and that connectome topology with random weights is \"almost always within the range defined by the other two architectures\". This project had to actively suppress its substrate's recurrence to stop it exploding: the TmY4 -> TmY4 -> TmY5a -> T2a loop reached 1e8 in 2 s, fixed by density strides that cut the network from FlyVis's 45,669/1.51M to 31,526 neurons/829k edges (step2 report §3.2).\n\n(5) The substrate does not yet compute the one thing the fly optic lobe is known for. Model zero's T4/T5 direction selectivity index is 0.020 against FlyVis's 0.391 (runs.jsonl:12-13; step2 report §2.2) — a 20x deficit — with no CT1 edges at all (ISSUES.md:30, 37-61), Tm4 column tags wrong in 47% of cells (ISSUES.md:31, 63-84), and only 0.869 of FlyVis type pairs recovered at Spearman 0.752 (runs.jsonl:4-5). A null result on this export cannot be told apart from \"our export is too lossy to show the effect\", so the experiment is uninterpretable in both directions.\n\nThe image-generator half is additionally redundant rather than wrong: the substrate's visual dimension is 721 hexals ~ 27x27, the same order as MNIST's 784 and 1,091x smaller than 512x512x3, and roadmap item 4 already reconstructs that 721-hexal frame at PixCorr 1.000 by ridge regression (runs.jsonl:21; step4 report §3). \"Generate from a frozen substrate with a trained readout, input removed\" is roadmap item 7, which already has a design and a control. There is no third experiment here.\n\nWhat would have falsified this attack: a robust, large advantage for real fly wiring over degree-matched nulls on a fly connectome at scale. The one paper that ran that control returned 1.6% with overlapping error bars, and the one peer-reviewed multi-species degree-preserving test returned the fly as its only non-significant species. Note honestly that the cross-species preprint bioRxiv 2025.10.29.685101 reports p < 0.01 for all species including the fly — but with a weight-distribution-preserving shuffle and, in its own methods, \"no explicit degree-matched null model is tested\", which is precisely the weaker null that inflates the effect.",
 "required_changes": [
  "Delete token prediction / the LLM spark from ROADMAP.md:166-169. It is not recoverable by scope change: a frozen substrate contributes 0 trainable parameters and the readout at GPT-2 vocabulary is 2.295e9 params, 18.5x GPT-2 small. Any language ability would be the wrapper's, as nftechie/FLM already demonstrates at 0.023% fly parameters.",
  "Replace 'shuffled-connectivity control of the same size' (ROADMAP.md:168) with degree- AND weight-distribution-preserving rewiring. Size matching alone reproduces the Erdős–Rényi artifact that gives FlyGM its spurious 15.1x headline, against 1.6% for the honest null.",
  "Implement an actual wiring shuffle. flydream/model/zero.py:72-78 shuffle_strengths permutes parameters, not connectivity, and diverges to inf 3/3 (runs.jsonl:16), so the control named in the roadmap has neither code nor a computable value today.",
  "Gain-normalise both arms to the same spectral radius and sweep it (alpha ~ 0.7-1.2). Every advantage in the literature is criticality-conditional (conn2res alpha_peak 0.9-1.15; PMC12109256: at SR 0.25 the classical ESN wins), and this project's substrate was deliberately detuned by density strides to stop a 1e8 runaway.",
  "Use an ensemble of nulls, not one. conn2res uses 500 rewired nulls; a single seeded shuffle cannot resolve the single-digit-percent effect the literature predicts.",
  "Pre-register the readout parameter budget and report it beside every number, so the readout cannot be the model. FlyGM cut its readout to 22e3 for this reason; fly_ocr's 266k-parameter readout on character recognition is already larger than a model that would solve the task with no fly at all.",
  "Fix or bound the substrate first (ISS-0002 missing CT1, DSI 0.020 vs 0.391) or accept that neither outcome is interpretable — a null cannot be distinguished from a lossy export.",
  "If anything survives, rescope it to a bounded reservoir-capacity measurement (memory capacity, NARMA-10, Lorenz) against degree-matched nulls, reported as a capacity number beside its control per AGENTS.md — not as 'an image generator' (redundant with item 4 and capped at 721 hexals) and not as 'an LLM'.",
  "Keep the 'after item 4' gate at ROADMAP.md:169 and harden it: item 4 has produced zero decodability numbers so far (step4 report §6) and its Sintel run is still mid-flight (pairs.npz present, map.csv absent), so this leg is a distraction until the project's central deliverable has its first number beside its control."
 ],
 "numbers": [
  "Fruit fly is the ONLY species whose connectome fails its degree-preserving null: p = 0.11 at alpha_peak = 0.9, vs mouse p < 0.002, rat p < 0.002, macaque p < 0.002, human p = 0.002, against 500 nulls preserving density and degree sequence — Suárez et al., conn2res, Nature Communications 2024, https://pmc.ncbi.nlm.nih.gov/articles/PMC10803782/ (verbatim: 'At peak memory capacity, biologically-informed connectomes perform significantly better than rewired nulls, except for the fruit fly')",
  "Caveat on that citation: the conn2res fly network is 49 nodes, 1,950 edges, 82.9% dense, fluorescence-weighted, coarse-grained from Chiang et al. 2011 Current Biology — not a sparse optic lobe (node table in arXiv 2606.09902, Table III)",
  "Degree-preserving null vs real FlyWire connectome on locomotion, position error 0.0370+-0.002 vs 0.0364+-0.002 (1.6% apart, error bars overlap); angle error 13.55+-0.69 vs 8.29+-0.21 (1.63x); Erdős–Rényi non-degree-matched 125.36+-8.96 (15.1x) — FlyGM, arXiv 2602.17997 Table 1",
  "Exact wiring adds only a few percent over degree/weight-matched ensembles on gross dynamics: 'gain and effective dimensionality within a few percent of the ensemble'; exact wiring matters only for input routing (activity confined to 1/5 of the core vs 2/3 for the ensemble) and mushroom-body modes; 2,825-neuron strongly connected core — Therianos, arXiv 2606.17745",
  "Frozen-substrate LLM readout: 45,669 x 50,257 = 2.295e9 trainable params = 18.5x GPT-2 small (124M), 69.6x TinyStories-33M; on model zero 31,526 x 50,257 = 1.584e9 = 12.8x GPT-2 small (neuron counts ROADMAP.md:36-37, research_notes/.../flyvis_and_simulators.md:14)",
  "Input embedding into the 721-hexal port: 50,257 x 721 = 36.2M params, alone 1.10x TinyStories-33M (hexal count from config.toml:11-12, extent 15)",
  "Most charitable byte-vocab config still leaves an 11.88M-param trained wrapper (FlyVis N) or 8.26M (model zero N) around a 0-param fly; TinyStories floor for coherent English is ~1-28M params (Eldan & Li 2023, arXiv 2305.07759)",
  "FlyVis has 734 free parameters total (65 resting potentials + 65 time constants + 604 synapse scalings) — 168,937x fewer than GPT-2 small, 0.073% of TinyStories-1M (research_notes/Коннектом мухи и план проекта/flyvis_and_simulators.md:19)",
  "MaleCNS whole CNS as a sparsity mask (weights trainable, i.e. NOT a frozen substrate) = 25.6M params = 0.78x TinyStories-33M; model zero 829k = 0.83x TinyStories-1M (research_notes/.../malecns.md:8; ROADMAP.md:36-37)",
  "Substrate memory horizon: tau = 50 ms init (flyvis_and_simulators.md:19) at dt = 20 ms (config.toml:34) gives per-step retention 0.670, below 1% after 12 steps = 0.24 s — ~100x short of the O(1e3) tokens token prediction needs; total capacity bounded by number of independent state variables, N <= 31,526 (Dambre et al. 2012, Sci Rep 2:514)",
  "Project's own substrate deficit: T4/T5 DSI 0.020 (MaleCNS model zero) vs 0.391 (FlyVis), a 20x deficit, per-model 0.034/0.026/0.000 — reports/runs.jsonl:12-13, step2 report §2.2",
  "Project's only shuffle control diverged to inf in 3/3 members under both protocols — reports/runs.jsonl:16; flydream/model/zero.py:72-78 permutes per-type-pair strength parameters, not connectivity",
  "Substrate recurrence had to be suppressed: TmY4 -> TmY4 -> TmY5a -> T2a loop reached 1e8 in 2 s; density strides cut 45,669/1.51M to 31,526 neurons/829k edges — step2 report §3.2",
  "Export fidelity limits: 0.869 of FlyVis type pairs recovered, Spearman 0.752, sign agreement 0.951 (runs.jsonl:4-6); CT1 has 56,517 outgoing rows but only 8 reach typed right-lobe neurons, so all CT1 pathways are absent (ISSUES.md:30, 37-61); Tm4 column tag disagrees with its synapse mass in 47% of cells (ISSUES.md:31, 63-84)",
  "Image-generation ceiling: 721 hexals ~ 27x27, same order as MNIST's 784 and 1,091x smaller than 512x512x3; item 4 already reconstructs the 721-hexal frame at PixCorr 1.000 / R2 1.000 / identification 1.00 of chance 0.05 from photoreceptors (runs.jsonl:21, step4 report §3)",
  "Prior art proving the wrapper does the work: nftechie/FLM = 278k adapter into Liquid AI 1.2B LM, fly share 0.023% of trainable params; jerryjliu/fly_ocr = frozen MaleCNS reservoir with a 266k-param decoder claiming 87.6% — both unverified hobby code, flagged as such at reports/Коннектом мухи и план проекта.md:165",
  "Drosophila-ESN study: advantage is overfitting-resilience at beta = 1e-6 only, and 'in the case of a spectral radius of 0.25, the classical ESN consistently performs better than the connectome-based ESN'; connectome topology with random weights lies 'almost always within the range defined by the other two architectures' — https://pmc.ncbi.nlm.nih.gov/articles/PMC12109256/",
  "Reservoir size, not topology, drives the gain: r = 0.97, p = 0.001 between N and memory-capacity improvement; density correlates negatively r = -0.83, p = 0.042 — arXiv 2606.09902",
  "Random topology is a strong baseline in ML generally: a randomly pruned Wide ResNet-50 subnetwork sparsely trained matches dense Wide ResNet-50 accuracy on ImageNet — Liu et al., ICLR 2022, arXiv 2202.02643",
  "The fly's own celebrated ML export is that its wiring is random: Kenyon cells sample projection neurons in an apparently random combination (Caron et al. 2013, Nature 497:113), the basis of FlyHash's random-projection LSH (Dasgupta, Stevens & Navlakha 2017, Science)"
 ],
 "unchecked": [
  "data/decode/2026-09-18_decode_sintel_flyvis/map.csv DOES NOT EXIST — the directory holds only pairs.npz (1,286,868,990 bytes, mtime 2026-09-18 05:04), so that run is mid-flight. The task's stated Sintel facts (sample-shuffle floor 'a uniform 0.117', time-shuffle 'ranges 0.12-0.59') appear nowhere in the repository; grep for 0.117 / 0,117 across *.md, *.jsonl, *.csv, *.json returns only sklearn and matplotlib sample data. I could not verify them and did not rely on them.",
  "The only Sintel numbers on disk are data/decode/smoke_sintel/map.csv — 2 cell types, 24 samples — where L1 real-fit PixCorr is 0.656 against shuffled_samples -0.108 and shuffled_time -0.014, and T4a real-fit PixCorr is -0.002 (i.e. T4a decodes nothing there). Too small to draw on; the photoreceptor PixCorr 1.000 I cite is from the edges run (runs.jsonl:21), not Sintel.",
  "FlyGM (arXiv 2602.17997) does not report its neuron count, synapse count, or trainable-parameter count, so I cannot determine how much of its 1.63x angle-error gap against the degree-preserving null is readout capacity rather than wiring.",
  "I could not decode the body of arXiv 2606.17745 (PDF streams were FlateDecode-compressed); I used its abstract only, so the 'within a few percent' and 1/5-vs-2/3 routing figures are abstract-level, not verified against its tables.",
  "The hobby-project parameter counts (fly_ocr 266k / 87.6%, FLM 278k / Liquid AI 1.2B, swat-or-buy) are unverified — reports/Коннектом мухи и план проекта.md:165 states explicitly that the numbers come from list descriptions and were not checked. I did not open those repositories.",
  "No cost estimate exists anywhere in the repository for the substrate experiment, so I could not price it against roadmap items 3-7 or quantify the opportunity cost beyond noting that item 4 has produced no decodability number yet (step4 report §6) and items 3, 5, 6, 7 are unstarted.",
  "I did not test whether a degree-preserving rewiring of data/ol/filters_R.json would itself diverge under the transplanted parameters. Given that the far milder strength permutation went to inf in 3/3, this is a live risk for the proposed control and I have no measurement of it.",
  "I did not check whether the FlyVis paper reports task performance for a connectome-constrained versus unconstrained network of matched size; PMC11525180 reports no randomized-connectome control at all (only random-parameter and reduced-connectome-knowledge ablations), so the 'does the connectome constraint help or cost task performance' question has no answer from the reference this project measures itself against."
 ]
}